# PetSitters

## Описание

Сервис предоставляет возможность заказа услуги на выгул и уход за животными.

## Типы пользователей

**Клиент** — может регистрировать своих животных в системе и выставлять заявки на выгул или уход.

**Петситтер** — может выполнять заявки, оставленные клиентами.

## Функциональность

- ✅ Регистрация и аутентификация клиентов и петситтеров
- ✅ Регистрация животных (CRUD операции)
- ✅ Создание и выполнение заявок (полный workflow)
- ✅ Система отзывов для петситтеров (с автообновлением рейтинга)

## API

### 📚 Swagger документация

После запуска приложения, интерактивная документация API доступна по адресу:
**http://localhost:3110/api**

### Основные эндпоинты

#### Аутентификация
- `POST /auth/register` - Регистрация нового пользователя (клиент или петситтер)
- `POST /auth/login` - Вход в систему

#### Животные
- `POST /pets` - Зарегистрировать животное (только для клиентов) 🔒
- `GET /pets` - Получить список всех животных
- `GET /pets/my` - Получить своих животных 🔒
- `GET /pets/:id` - Получить информацию о животном
- `PATCH /pets/:id` - Обновить информацию 🔒
- `DELETE /pets/:id` - Удалить животное 🔒

#### Заявки
- `POST /requests` - Создать заявку (только клиенты) 🔒
- `GET /requests` - Список всех заявок (с фильтром по статусу)
- `GET /requests/pending` - Доступные заявки для петситтеров
- `GET /requests/my` - Мои заявки 🔒
- `GET /requests/statistics` - Статистика по заявкам 🔒
- `GET /requests/:id` - Информация о заявке
- `POST /requests/:id/accept` - Принять заявку (петситтеры) 🔒
- `PATCH /requests/:id/status` - Обновить статус 🔒
- `DELETE /requests/:id` - Отменить заявку (клиенты) 🔒

#### Отзывы
- `POST /reviews` - Оставить отзыв (только клиенты) 🔒
- `GET /reviews` - Список всех отзывов (с фильтром)
- `GET /reviews/petsitter/:id` - Отзывы о петситтере
- `GET /reviews/petsitter/:id/statistics` - Статистика отзывов
- `GET /reviews/:id` - Информация об отзыве
- `PATCH /reviews/:id/toggle-visibility` - Скрыть/показать отзыв (петситтеры) 🔒

🔒 - требуется авторизация

## Настройка окружения разработки

Создать файл в корне репозитория `/local.env`, используя в качестве примера файл `/local.env.sample`

## Сборока
```bash
GIT_SHA1=$(git rev-parse HEAD)
docker build . --build-arg GIT_SHA1=$GIT_SHA1 --target petsitters --tag ptstr/petsitters:0.0.1
```

## Запуск приложения

```bash
docker compose --env-file ./local.env up -d --wait
```

После запуска приложение будет доступно:
- **API:** http://localhost:3110
- **Swagger документация:** http://localhost:3110/api
- **Mongo Express:** http://localhost:8081 (admin/admin123)